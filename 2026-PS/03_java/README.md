## 🎯 Autoavaliacao

Conceito pretendido: B

Justificativa:

- Ex 1 e 2 (arrays + métodos): implementados nos metodos `calcularMedia()` e `contarAprovados()`.
- Ex 3 (ArrayList): implementado nos métodos `adicionarProduto()` e `listarProdutos()`.
- Ex 4 (sobrecarga): implementado nos métodos `maiorValor(int[])` e `maiorValor(int, int)`.
- Ex 5 (integração): o método `exibirBoletim()` reutiliza `calcularMedia()` e `contarAprovados()`, sem repetir a lógica.
- Desafio (nível A): implementado no método `contarAcimaDaMedia()`.
- Autoavaliação: esta seção do README.
